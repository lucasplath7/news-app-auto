services:
  news-app-auto:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-/api}
    container_name: news-app-auto
    restart: unless-stopped
    networks:
      - web

networks:
  web:
    external: true